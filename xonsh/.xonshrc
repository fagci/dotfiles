# $XONSH_SHOW_TRACEBACK = True
if 'TERMUX_VERSION' in ${...}:
    launch = p'$PREFIX/../home/storage/shared/termuxlauncher/.apps-launcher'
    if launch.exists():
        source-bash @(launch)

def _rtn():
    if __xonsh__.history and __xonsh__.history.rtns[-1] > 0:
        return __xonsh__.history.rtns[-1]

$PROMPT_FIELDS['rtn'] = _rtn
if 'SSH_CLIENT' in ${...}:
    $PROMPT='{RED}{rtn:{} }{GREEN}{user}@{hostname} {CYAN}{cwd}{WHITE}{BACKGROUND_BLACK}{curr_branch: {} }{RESET}{YELLOW}⪫{RESET} '
else:
    $PROMPT='{RED}{rtn:{} }{CYAN}{cwd}{WHITE}{BACKGROUND_BLACK}{curr_branch: {} }{RESET}{YELLOW}⪫{RESET} '

aliases['l'] = 'ls'
aliases['ll'] = 'ls -l'
aliases['la'] = 'ls -a'

aliases['ga'] = 'git add'
aliases['gl'] = 'git pull'
aliases['gp'] = 'git push'
aliases['gcam'] = 'git commit -am'
